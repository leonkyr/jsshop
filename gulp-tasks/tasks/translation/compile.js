// 'use strict';

// var gulp = require('gulp-help')(require('gulp')),
//     gettext = require('gulp-angular-gettext');

// gulp.task('translation:compile', 'Compile translation for use by UI', function (done) {
//     gulp.src('translations/**/*.po')
//         .pipe()
//         .pipe(gulp.dest('app/translations'))
//         .on('finish', function () {
//             done();
//         });
// });